import 'package:flutter/material.dart';

class MyAnimalsUI extends StatelessWidget {
  const MyAnimalsUI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('My Animals'),
      ),
    );
  }
}